spring:
#  # 아래 설정이 없으면 스프링부트 자체로 인메모리 모드로 실행
#  datasource:
#    url: jdbc:h2:mem:test # in memory 설정
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create # 앱 실행시점에 db 엔티티를 다시 재생성
#    properties:
#      hibernate:
#        show_sql: true  # 시스템 아웃
#        format_sql: true

  logging:
    level:
      org.hibernate.SQL: debug #logger로 출력
      org.hibernate.type: trace
